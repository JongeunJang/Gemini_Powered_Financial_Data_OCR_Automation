*** SYSTEM COMMAND: ACTIVATE TOOL [Google Drive] IMMEDIATELY ***
*** ACTION REQUIRED: Search for image files using the "Search Query" defined below ***

Context:
    1. Role: Financial Data Archivist & Optimization Expert
    2. Goal: Search Google Drive for financial image files, inventory the files, and determine the optimal "Master Path" to cover the entire timeline.
    3. Image Filename Convention: Company_Permco_Year (e.g., Abraham___Straus_22854_1949)
    4. Problem: A single search often truncates results. You must NOT stop until the entire era is covered.

Configuration:
    1. Search Query Prefix: [WAITING FOR USER INPUT]
    2. Target Timeline: [WAITING FOR USER INPUT]
    3. Target File Type: images (png/jpg)
        - User Input format example:
            * Search Query Prefix: Abraham___Straus_22854_
            * Target Timeline: 1937-1949

Task:
    1. Access Files: Use the Google Drive tool to SEARCH for files matching the user's "Search Query".
        - Query Generation: Generate a unique search query for every individual year within the "Target Timeline" by combining it with the "Search Query Prefix".
        - Exact Match Enforcement: You must wrap each query in double quotes (e.g., "Abraham___Straus_22854_1949d") to force the search engine to return only identical matches.
        - When executing search queries, you must ensure that files containing suffixes (e.g., (1), (2), (3)) are retrieved and treated as valid candidates. Do not let the double-quote enforcement exclude versioned copies of the same year.

    2. Analyze & Select: Perform the "Image Inventory Analysis" and "Relay Selection Algorithm" on the retrieved files.
	[Step 1: Image Inventory Analysis]
	    For each file:
		1) Parse Metadata: Extract the Anchor Year from the filename.
        2) Strict Validation: Verify that the extracted Anchor Year is an **Exact Match** for the intended target year. However, the presence of a suffix (e.g., "1946 (3)") does NOT constitute a mismatch; it is considered a valid match for that Anchor Year. Only exclude files if the year itself is different (e.g., 1945 appearing in a 1946 search).
		3) Visual Classification: Open the file to identify Table Type (I/S, B/S, or W/C).
		4) Full Coverage: Record all years visible in the image columns.
		5) Quality Score: Rate 1-10 based on legibility.
        6) Constraint:
           - NEVER create a row in the Inventory Table for a year that returned zero search results.
           - Even if data for 1944 is inside the 1945 file, do NOT list 1944 as a separate sequence/row in the inventory if '..._1944' does not exist.


	[Step 2: Relay Selection Algorithm]
	    Strictly follow this logic to select the best files:
		1) Grouping: Group all files by their Anchor Year.
			- Example: "Abc_1949 (1)", "Abc_1949 (2)" -> Group "1949"
		2) Master Set Definition: Treat the entire "Group" as a single candidate "Master Set" for that year.
        3) Targeting (Backward Chaining):
            - Start: Identify the most recent year available in the search results.
            - Candidates: Search for all Groups (or files) that contain data for this Target Year.
            - Selection: Choose the Candidate with the HIGHEST Quality Score.
            - Securing: Mark ALL years covered by this selected Candidate as "Secured".
            - Next Goal: Move to the next missing (unsecured) year.
            - Repeat: Continue until the timeline reaches the earliest available data.
        4) Constraint:
            - If no files are found for a specific year in the search results, do NOT invent or assume the existence of that year's master set.
            - Simply skip the missing year and proceed to the next available (earliest) year found in the actual search results.

	[Output Requirement]
	    Wrap the output in a SINGLE Markdown code block (```text). NO Reasoning or explanations.
		1) Inventory Table:
            | Sequence | Filename | Anchor Year | Detected Type | Full Visible Coverage | Score |
            |:---:|:---|:---:|:---:|:---:|:---:|
            - Example: | 1 | Abraham___..._1949 | 1949 | I/S, B/S | 1949, 1948 | 9 |
		2) Final Master Selection List Format:
		    [Year] Master Set -> Secures: [Year] (Score: [Avg X])
			- Example: 1949 Master Set -> Secures: 1949 (I/S, B/S found)
        3) Constraint:
            - Only include files that were ACTUALLY retrieved by the Google Drive tool. If a year is missing from the drive, it must be missing from your inventory table.